# 개발 계획 v3

## 0. 문서 목적
- 이 문서는 `development_plan_v2.md`의 모순과 오래된 이행 체크리스트를 걷어내고, 현재 구현 기준의 단일 기준 문서로 사용한다.
- 앞으로의 작업은 "예전 구조를 어떻게 옮길지"가 아니라 "지금 있는 구조 위에서 무엇을 완성할지" 기준으로 적는다.
- 구조 원칙은 `docs/project_structure_principles.md`를 따른다.

## 1. 현재 제품 정의
- 현재 앱은 `Town / Workshop / Characters / Battle` 4탭 구조다.
- 제품의 중심 루프는 다음 순서다.
  1. `Battle`에서 골드, 정수, 재료, 캐릭터 XP를 획득한다.
  2. `Town`에서 상점 구매, 포션 판매, 장비 제작, 용병 고용, 마을 스킬트리를 진행한다.
  3. `Workshop`에서 재료 추출, 포션 양조, 제작, 장비 인챈트, 호문쿨루스 부화, 작업실 스킬트리를 진행한다.
  4. `Characters`에서 용병/호문쿨루스 성장, 장비 관리, 배치 상태를 확인한다.
- 전역 시간 진행은 `app/session`의 sync 파이프라인과 시간 가속 버튼으로 처리한다.

## 2. 현재 구조 기준

### 2.1 앱 구조
- 앱 셸은 `lib/app/app.dart`에 있다.
- 전역 세션 상태 저장소와 sync 파이프라인은 현재 `lib/app/session/*`에 있다.
- 기능별 코드는 `lib/features/<feature>` 아래 `presentation / domain / data`로 나뉜다.
- 재사용 UI와 테마는 `lib/common/*`에 둔다. UI 표면과 시트/모달 기준은 `docs/ui_design_system_guidelines.md`를 따른다.

### 2.2 상태 소유권
- `Town`은 상점, 판매, 장비 제작, 용병 고용, 마을 스킬트리의 소유자다.
- `Workshop`은 추출, 포션 양조, 원소 + 재료 기반 제작, 인챈트, 부화, 작업실 스킬트리의 소유자다.
- `Characters`는 성장, 장비 장착, 배치 표현의 소유자다.
- `Battle`은 자동 전투, 원정 상태, 전투 보상, 스테이지 배치의 소유자다.
- feature 비즈니스 규칙은 각 feature `domain`에 있고, 전역 세션은 규칙을 직접 소유하지 않는다.

### 2.3 제작 경계
- Town 장비 제작은 골드 + 재료를 사용해 장착 가능한 완성 장비를 만든다.
- Workshop 제작은 원소 + 재료 + 정수 / 신비를 사용해 승급 재료, 촉매, 특수 부품 같은 성장 매개체를 만든다.
- 포션은 Workshop `양조`로 분리한다.
- 장착하는 물건은 Town, 성장 / 강화 / 해금 매개체는 Workshop이 담당한다.

## 3. 재화와 자원 규칙

### 3.1 현재 재화 정의
- 골드 (`Gold`)
  - Town 경제 재화
  - 현재 획득처: 전투 보상, 포션 판매
  - 현재 사용처: 상점 구매, 상점 강제 갱신, 용병 고용, 마을 스킬트리 일부 비용
- 정수 (`Essence`)
  - Workshop 운영 재화
  - 현재 획득처: 전투 보상
  - 현재 사용처: 호문쿨루스 부화
- 명성 (`townInsight`)
  - Town 성장 재화
  - 현재 획득처: 포션 판매
  - 현재 사용처: 마을 스킬트리
- 신비 (`arcaneDust`)
  - Workshop 성장 재화
  - 현재 획득처: 재료 추출 완료 보상
  - 현재 사용처: 작업실 스킬트리, 호문쿨루스 부화
- 원소 재고 (`extractedTraitInventory`)
  - Workshop 전용 제작 자원
  - 현재 획득처: 재료 추출
  - 현재 사용처: 포션 양조, 작업실 스킬트리 일부 비용, 호문쿨루스 부화 원소 비용
  - 확장 계획: [Workshop 양조 / 제작 분리 계획](./workshop_crafting_expansion_plan.md)에 따라 원소 + 재료 기반 제작 비용으로도 사용할 수 있게 한다.
- 재료 재고 (`materialInventory`)
  - 공통 재료 자원
  - 현재 획득처: Town 상점 구매, Battle 드롭
  - 현재 사용처: 장비 제작, 재료 추출, 호문쿨루스 부화, 캐릭터 티어업
- 다이아 (`diamonds`)
  - 현재 HUD 플레이스홀더
  - 실제 소비처와 확장 규칙은 아직 미구현

### 3.2 현재 구현 기준의 source / sink 매트릭스

| 자원 | 주요 source | 주요 sink | 비고 |
| --- | --- | --- | --- |
| 골드 | 전투, 포션 판매 | 상점 구매, 강제 갱신, 용병 고용, Town 스킬트리 | 실패 전투는 무보상이며 패널티도 없다 |
| 정수 | 전투 | 부화 | 현재 Workshop 일반 행동 비용으로 넓게 쓰이지 않음 |
| 명성 | 포션 판매 | Town 스킬트리 | Town 전용 성장 재화 |
| 신비 | 추출 완료 수령 | Workshop 스킬트리, 부화 | Workshop 전용 성장 재화 |
| 원소 | 추출 | 제조, Workshop 스킬트리, 부화 | 통화라기보다 제작 재고에 가깝다 |
| 재료 | 상점, 전투 | 제작, 추출, 부화, 티어업 | 양 feature에서 공유 사용 |

### 3.3 현재 문서화 기준
- `Town 재화 = 골드만`, `Workshop 재화 = 정수만` 같은 단순 규칙은 더 이상 사용하지 않는다.
- 현재 구현은 "기본 경제 재화"와 "성장 전용 재화"가 분리된 구조다.
- 현재 기준에서 중요한 구분은 아래와 같다.
  - `Town` 기본 경제 축: 골드
  - `Town` 성장 축: 명성
  - `Workshop` 운영 축: 정수
  - `Workshop` 성장 축: 신비
  - `Workshop` 제작 축: 원소

## 4. 시스템별 현재 상태

### 4.1 Town
- [x] 일반 상점 / 촉매 상점 구매
- [x] 상점 강제 갱신
- [x] 상점 자동 갱신 스케줄
- [x] 포션 판매와 `골드 + 명성` 반영
- [x] 장비 제작 큐 등록 / 완료 수령
- [x] 용병 고용
- [x] Town 스킬트리 진입 / 강화
- [x] 상점별 구매 제한 / 품절 / 재입고 정책 적용
  - 일반 상점: 상품별 주기 한도 20개 / 15분마다 재입고
  - 촉매 상점: 상품별 주기 한도 8개 / 30분마다 재입고
  - 재고 0개는 품절로 표시하고 자동 / 강제 갱신 시 seed 재고로 복구한다.
- [x] Town 스킬트리 연결선 / 강화 상세 시각화
- [-] 용병 해고 규칙은 아직 없다.
- [-] Town 안에서의 캐릭터 관리보다, 현재는 `Characters` 화면이 주 관리 화면에 가깝다.

### 4.2 Workshop
- [x] 작업실 단일 큐 운영
- [x] 추출 등록 / 수령
- [x] 포션 제조 등록 / 수령
- [x] [Workshop 양조 / 제작 분리 계획](./workshop_crafting_expansion_plan.md) 기준으로 포션 양조와 원소 + 재료 제작 경계 분리
- [x] [Workshop 양조 / 제작 분리 계획](./workshop_crafting_expansion_plan.md) 기준으로 승급 재료 제작 경로 추가
- [ ] [Workshop 양조 / 제작 분리 계획](./workshop_crafting_expansion_plan.md)의 발견형 포션 양조 기준으로 `원소 조합 / 비율 실험 -> 결과 발견 -> 레시피북 반복 양조` 흐름 전환
- [x] 장비 인챈트 등록 / 수령
- [x] 호문쿨루스 부화 등록 / 수령
- [x] 재료 / 원소 / 포션 재고 조회
- [-] 작업실 보조 슬롯 배치는 제품 기능에서 제외한다. 현재 대시보드 진입점은 제거했고, 남은 domain/state 로직은 추후 별도 정리한다.
- [x] Workshop 스킬트리 진입 / 강화
- [x] `정수 / 신비` HUD 노출
- [x] Workshop 보유 / 선택 자원 그리드 표시 규칙 적용. 남은 세부 판단은 [Workshop 디자인 현황 / 이슈](./workshop_design_issues.md)를 따른다.
- [x] Workshop 스킬트리 연결선 / 강화 상세 시각화
- [-] 현재 제품 기준의 큐 슬롯 규칙은 "초기 4칸 + 스킬트리 보너스"다. 보조 슬롯 보너스 로직은 제거 예정 잔존 코드다.
- [ ] 다이아 기반 슬롯 확장 규칙은 아직 없다.

### 4.3 Characters
- [x] 용병 + 호문쿨루스 통합 목록
- [x] 상세 시트
- [x] 레벨 / 랭크 / 티어 성장 반영
- [x] 장비 장착 / 해제
- [x] 전투 / 작업실 배치 상태 표시
- [-] 현재 화면은 서브탭 분리형이 아니라 통합 리스트형이다.
- [ ] 개체 정리 정책은 아직 없다.

### 4.4 Battle
- [x] 스테이지별 편성 시트
- [x] 스테이지별 원정 시작 / 정지 / 수령
- [x] `탐색 -> 전투 -> 다음 탐색`, 전멸 시 `복구 -> 탐색` 상태 흐름
- [x] 전투 결과에 따른 `골드 / 정수 / 재료` 누적 수령 반영
- [x] 성공 encounter XP 즉시 반영
- [x] 전투 결과 상세 시트 / 최근 결과 로그
- [x] 연속 run 기준 전투 현황 시트
- [x] 전투 현황 시트의 MP / 보호막 / 상태이상 / active modifier 표시
- [x] 스테이지별 적 구성 / 드롭 근거 표시
- [x] 적 / enemy set / 스테이지 데이터 정규화
- [x] 이전 stage 무실패 연속 승리 기반 스테이지 해금 판정
- [x] stage별 포션 loadout 상태 / UI / 소비 규칙 연결
- [x] Stage 1~5 드롭 / 보상 / 적 세트 구체화
- [x] [Stage Unlock 설계](./battle/stage_unlock_design.md) 기준으로 이전 stage 무실패 연속 승리 해금 구현
- [x] 일반 몬스터 종류 / 조합 다양화
- [x] 적별 행동 패턴 확장
- [x] 보스급 적은 일반 enemy catalog / skill / passive / modifier 경계로 흡수

### 4.5 Session / Offline
- [x] 세션 스냅샷과 상태 적용
- [x] 라이브 sync 파이프라인
- [x] 시간 가속
- [ ] 영속 저장소는 아직 없다.
- [ ] 앱 종료 후 복귀 복원은 아직 없다.
- [ ] 오프라인 통합 처리와 cap 검증은 아직 없다.
- [-] 데이터 테이블과 카탈로그가 계속 변하는 단계라 실제 저장 구현은 보류하고, 현재는 저장 경계와 버전 정책만 설계 대상으로 본다.

### 4.6 공통 인벤토리 / 자원 표현
- [x] [인벤토리 / 보유 자원 표시 범위](./inventory_resource_ux_scope.md) 기준으로 표시 범위 확정
- [x] 재료 / 원소는 현재 스택형 자원 기준으로 표시 범위를 정리했다.
- [x] 확정된 범위를 Town 판매 선택, Town 대장간 보유 장비, Workshop 재료/포션/장비 선택, Characters 장비 장착 화면에 적용했다.

## 5. v2에서 정리된 모순

### 5.1 재화 규칙 모순 제거
- `v2 3.0.1`의 "`Town = 골드`, `Workshop = 정수` 단일 재화 구조"는 현재 구현과 맞지 않는다.
- 현재 구현은 아래처럼 동작한다.
  - `Town`: 골드 + 명성
  - `Workshop`: 정수 + 신비 + 원소
- 특히 아래 내용은 `v2`의 표현이 잘못 단순화한 부분이다.
  - 장비 제작은 현재 골드가 아니라 재료를 소비한다.
  - 작업실 스킬트리는 신비 + 원소를 소비한다.
  - 호문쿨루스 부화는 정수 + 신비 + 재료 + 원소를 소비한다.

### 5.2 구 UI 마이그레이션 체크리스트 제거
- `Weapons` 단일 화면 기준의 역할 정의는 현재 상태를 설명하지 못한다.
- 현재 앱은 이미 `Town / Workshop / Characters / Battle` 4탭 기준으로 동작한다.
- 따라서 `v3`에서는 "예전 화면을 어디로 옮길지"가 아니라 "현재 각 탭이 무엇을 책임지는지"만 유지한다.

### 5.3 캐릭터 화면 상태 정정
- `v2`의 "용병 / 호문쿨루스 서브탭 분리 완료"는 현재 구현과 맞지 않는다.
- 현재는 두 타입을 한 화면에서 통합 리스트로 보여준다.

### 5.4 큐 슬롯 규칙 정정
- `v2`의 "초기 2칸, 다이아 확장"은 현재 구현과 맞지 않는다.
- 현재 구현은 `기본 4칸 + Workshop 스킬트리 보너스`를 제품 기준으로 본다.
- 작업실 보조 슬롯 보너스 로직은 남아 있지만 기능 제거 예정 코드이므로 새 밸런스 기준에 포함하지 않는다.
- 다이아 확장은 아직 backlog다.

### 5.5 v2 잔재 보류 판단
- 용병 계약 상태는 현재 별도 계약 기간, 만료, 유지비 모델이 없으므로 구현 backlog에 넣지 않는다.
- 현재 필요한 용병 상태 표기는 전투 배치, 작업실 배치 불가, 대기 상태로 제한한다.
- 호문쿨루스 친화 특성은 현재 `originRecipeId`, 역할만 유지하고, 작업실 보조 효과에는 의존하지 않는다.
- 캐릭터 상세에서 배치 전환은 현재 주 흐름이 아니며, 배치 변경은 Battle 편성 시트 기준으로 처리한다.

### 5.6 작업실 보조 기능 제거 예정
- 작업실 보조 슬롯은 현재 제품 기능에서 제외한다.
- 대시보드 진입점은 제거되어 신규 플레이 흐름에서는 접근하지 않는다.
- 남아 있는 support feature, assignment state, queue slot bonus 연동은 추후 별도 제거 대상으로 둔다.
- 제거 전까지 새 기능이나 밸런스 기준은 작업실 보조 효과에 의존하지 않는다.

## 6. 현재 검증 상태

### 6.1 이미 커버된 축
- [x] 연금 추출 / 합성 규칙
- [x] 상점 강제 갱신 비용 증가
- [x] 제작 큐 상태 전이
- [x] 포션 타입 판정 / 품질 계산
- [x] 전투 XP 성장 / 랭크업 / 티어업
- [x] 작업실 추출 / 제조 / 인챈트 / 부화 위젯 및 use case
- [x] Town / Workshop 스킬트리 repository / use case / widget
- [x] 전투 편성 / 배치 상태

### 6.2 여전히 필요한 테스트
- [ ] 영속 저장소 도입 이후 복원 / 오프라인 통합 테스트
- [ ] 재화 부족 오류 메시지 구분 테스트
- [-] 스킬트리 리스펙은 현재 구현 보류라 테스트도 보류
- [x] 전투 해금 / 포션 loadout / fallback 회귀 테스트
- [x] 연속 run / 복구 회귀 테스트

## 7. 권장 다음 작업

### 7.0 P0 UI 폴리싱
- [ ] [UI 폴리싱 계획](./ui_polishing_plan.md) 기준으로 공통 UI 기준을 재점검한다.
- [ ] Workshop 폴리싱을 우선 진행한다.
- [ ] Battle / Town / Characters 순서로 반복 확인 화면을 정리한다.
- [ ] 상태어, 자원명, 버튼 라벨을 통일한다.

### 7.1 P1 전투 완성도
- [x] Stage 1~5 드롭 / 보상 / 적 세트 구체화
- [x] 연속 run 전투 규칙 / 전투 시트 재설계
- [x] [Stage Unlock 설계](./battle/stage_unlock_design.md) 기준으로 이전 stage 무실패 연속 승리 해금 구현
- [x] 일반 몬스터 종류 / 조합 다양화
- [x] 적별 행동 패턴 확장
- [x] 전투 현황 시트의 현재 효과 표시 보강
- [x] 보스급 적은 별도 전용 패턴 모델 없이 기존 적 정의로 구성
- [x] [전투 cycle 설계](./battle/combat/combat_cycle_design.md) 기준으로 action turn / lifecycle guard 적용
- [x] 연속 run / 복구 회귀 테스트 보강
- [-] 남은 전투 작업은 구조 구현이 아니라 stage / enemy / encounter 수치 튜닝으로 분리한다.

### 7.2 P2 재화 UX 정리
- [x] [재화 UX 정리](./currency_ux_guidelines.md) 기준으로 `골드 / 정수 / 명성 / 신비 / 원소` 부족 메시지 분리
- [x] 수급 / 소비 가이드를 UI 문구와 일치시킴
- [x] [인벤토리 / 보유 자원 표시 범위](./inventory_resource_ux_scope.md) 기준으로 공통 표시 범위 확정
- [x] 인벤토리 공통 표현 규칙은 Workshop에 우선 적용됐고, Town 포션 판매 선택 / Town 대장간 보유 장비 / Characters 장비 장착 선택까지 확장됐다.
- [-] 다이아는 현재 플레이스홀더로 유지한다. 실제 sink는 별도 설계 전까지 추가하지 않는다.

### 7.3 P3 저장 경계 / 버전 정책
- [x] [저장 경계 / 버전 정책](./save_version_policy.md) 기준으로 저장 대상과 비저장 대상을 구분
- [x] `saveVersion` / `contentVersion` 정책 정의
- [x] 테이블 의존 상태의 복원 / 폐기 기준 문서화

### 7.4 P4 성장 시스템 마감
- [x] [성장 시스템 마감 정책](./growth_system_closeout.md) 기준으로 스킬트리 리스펙 보류 결정
- [x] 용병 해고 또는 roster 정리 정책 보류 결정
- [x] 캐릭터 화면은 통합 리스트 유지로 재판단 완료
- [x] Town / Workshop 스킬트리 연결선과 강화 상세 시각화
- [x] [Workshop 양조 / 제작 분리 계획](./workshop_crafting_expansion_plan.md) 기준으로 티어업 승급 재료 획득 경로 연결
- [ ] 다음 필요사항: T3 이상 승급 재료 / 촉매 / 제작 레시피를 stage 확장과 함께 추가한다.
- [ ] 다음 필요사항: 티어업 재료 요구량과 공급량이 실제 진행 속도와 맞는지 점검한다.

### 7.5 P5 밸런스 데이터 외부화
- [x] [밸런스 데이터 외부화 사전 정리](./balance_data_externalization_plan.md) 기준으로 가격 / 드롭 / 제작시간 / 승급 재료 테이블 위치 정리
- [x] Battle stage / enemy / encounter 데이터를 외부화 가능한 DTO 경계로 정리
- [x] Battle stage / enemy / encounter / drop 데이터를 로컬 JSON asset에서 로딩
- [x] Town shop / equipment / mercenary / skill tree 데이터를 로컬 JSON asset에서 로딩
- [x] Workshop material / extraction / potion / craft / hatch / skill tree 데이터를 로컬 JSON asset에서 로딩
- [x] 상점별 구매 제한 / 품절 / 재입고 정책을 경제 테이블 기준으로 정의
- [x] [가격 / 드롭 / 제작시간 곡선과 경제 지표](./economy_balance_guidelines.md) 기준 문서화
- [x] 후반 점검형 전이 지표 정의: 평균 접속 간격, 접속당 처리 액션 수, 큐 대기시간
- [x] 경제 인플레이션 / 병목 / 이탈 지표 정의
- [x] shop / workshop recipe / extraction / hatch 데이터를 실제 외부화한다.
- [x] 외부화된 밸런스 테이블의 ID 안정성, 중복 ID, 참조 무결성, 비용 / 시간 유효성 검증을 추가한다.
- [x] 경제 / 밸런스 기준 문서를 실제 플레이 데이터 기준으로 갱신한다.
- [ ] 다음 필요사항: 가격 / 드롭 / 제작시간 / 승급 재료 공급량의 1차 튜닝 값을 확정한다.

### 7.6 P6 저장 구현
- [-] 저장 구현은 현재 보류한다.
- [-] 저장 DTO 초안 설계도 실제 저장 착수 전까지 보류한다.
- [ ] 데이터 테이블과 카탈로그 ID 체계가 안정화된 뒤 영속 저장소를 도입한다.
- [ ] 저장 데이터 마이그레이션 또는 무효화 정책을 확정한다.
- [ ] 앱 종료 / 복귀 복원과 오프라인 진행을 실제 구현한다.

## 8. 즉시 실행 우선순위
1. UI 폴리싱: 공통 UI 기준 재점검 후 Workshop부터 정리한다.
2. 가격 / 드롭 / 제작시간 / 승급 재료 공급량의 1차 튜닝 값을 확정한다.
3. 티어업 / 승급 루프 확장: T3 이상 승급 재료와 제작 레시피를 stage 확장에 맞춰 추가한다.
